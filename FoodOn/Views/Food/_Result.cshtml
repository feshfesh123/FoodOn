@model List<string>


<ul class="list-group">
    @foreach (var item in Model)
    {
        <a data-ajax="true" data-ajax-mode="replace" data-ajax-update="#info" asp-action="Recipe" asp-route-name="@item" class="list-group-item list-group-item-action">@item</a>
    }
</ul>
        
